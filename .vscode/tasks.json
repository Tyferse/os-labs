{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "lab1 build with CMake",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/lab1/build",
                "--config",
                "Debug"
            ],
            "group": "build"
        },
    ]
}