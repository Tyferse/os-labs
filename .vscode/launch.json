{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "gdb launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/${relativeFileDirname}/../build/clonger",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}/${relativeFileDirname}/../build",
      "environment": [],
      "MIMode": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "CMake build"
    }
  ]
}